import type { ReactElement } from "react";

interface JobProps {
  setInfoState: (info: ReactElement) => void;
}

const JobApplication = ({ setInfoState }: JobProps) => {
  return <div></div>;
};

export default JobApplication;
